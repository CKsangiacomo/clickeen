---
type Props = {
  title?: string;
  items: { title: string; body?: string }[];
};

const { title = 'Examples', items } = Astro.props;
---

<section class="ck-canvas">
  <div class="ck-inline ck-stack">
    <div class="heading-3">{title}</div>
    <div class="ck-row ck-examplesGrid__row">
      {items.map((item) => (
        <article class="ck-card ck-examplesGrid__card">
          <div class="ck-examplesGrid__thumb" aria-hidden="true" />
          <div class="ck-stack ck-examplesGrid__cardInner">
            <div class="label-l">{item.title}</div>
            {item.body ? (
              <div class="body-s ck-examplesGrid__body">{item.body}</div>
            ) : null}
          </div>
        </article>
      ))}
    </div>
  </div>
</section>

<style>
  .ck-examplesGrid__row {
    align-items: stretch;
    gap: var(--space-4);
  }

  .ck-examplesGrid__card {
    flex: 1 1 0;
    min-width: 260px;
    overflow: hidden;
  }

  .ck-examplesGrid__thumb {
    height: 180px;
    background: var(--color-system-gray-6);
  }

  .ck-examplesGrid__cardInner {
    padding: var(--space-6);
    gap: var(--space-2);
  }

  .ck-examplesGrid__body {
    color: color-mix(in oklab, var(--color-system-black), transparent 45%);
  }
</style>

