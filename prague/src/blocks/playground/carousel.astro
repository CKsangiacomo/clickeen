---
type Item = { title: string; body: string };

const items: Item[] = [
  { title: 'Card 1', body: 'Starts aligned to inline left edge.' },
  { title: 'Card 2', body: 'Scroll → cards exit right side.' },
  { title: 'Card 3', body: 'Keep scrolling → cards exit left side too.' },
  { title: 'Card 4', body: 'No wrapper clipping. Scroll container owns overflow.' },
  { title: 'Card 5', body: 'This is the “we won’t die” test.' },
];
---

<section class="ck-canvas">
  <div class="ck-inline ck-stack">
    <h2 class="heading-3">Carousel contract</h2>
    <p class="body-xxl" style="margin: 0; color: color-mix(in oklab, var(--color-system-black), transparent 56%);">
      The scroll viewport is the carousel element. Inline alignment comes from being inside <code>ck-inline</code>.
    </p>

    <div class="ck-carousel" aria-label="Carousel demo">
      <div class="ck-carousel__track">
        {items.map((item) => (
          <article class="ck-carousel__card">
            <div class="ck-carousel__cardInner">
              <div class="label-s">{item.title}</div>
              <div class="body-s" style="color: color-mix(in oklab, var(--color-system-black), transparent 45%);">
                {item.body}
              </div>
            </div>
          </article>
        ))}
      </div>
    </div>
  </div>
</section>

<style>
  .ck-carousel {
    overflow-x: auto;
    overflow-y: hidden;
    -webkit-overflow-scrolling: touch;
    scroll-snap-type: x mandatory;
    padding-block: var(--space-2);
  }

  .ck-carousel__track {
    display: flex;
    gap: var(--space-4);
    min-width: max-content;
    padding-block: var(--space-2);
  }

  .ck-carousel__card {
    flex: 0 0 360px;
    scroll-snap-align: start;
  }

  .ck-carousel__cardInner {
    border-radius: var(--control-radius-3xl);
    border: 1px solid color-mix(in oklab, var(--color-system-black), transparent 88%);
    background: var(--color-system-white);
    box-shadow: var(--shadow-floating);
    padding: var(--space-5);
    display: flex;
    flex-direction: column;
    gap: var(--space-2);
    min-height: 140px;
  }

  @media (max-width: 900px) {
    .ck-carousel__card { flex-basis: 280px; }
  }
</style>
