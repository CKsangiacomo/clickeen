Copy these lines into a new file at repo root named `.env.local` (gitignored),
or export them in your shell before running `pnpm build:prague:global`.

OPENAI_API_KEY=PASTE_KEY_HERE
OPENAI_MODEL=gpt-5.2


