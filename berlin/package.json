{
  "name": "@clickeen/berlin",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test:key-rotation": "tsx tests/run-signing-key-rotation.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:auth-boundary": "node ./verify-auth-boundary.mjs"
  },
  "dependencies": {
    "@clickeen/ck-contracts": "workspace:*",
    "@clickeen/ck-policy": "workspace:*",
    "@clickeen/l10n": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251228.0",
    "tsx": "^4.20.6",
    "wrangler": "^4.54.0"
  }
}
