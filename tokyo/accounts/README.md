# Tokyo Account Fixtures

This folder is documentation/fixture-only.

Real account-owned assets, instances, l10n, render state, indexes, exports, and deletion truth live in Tokyo-worker storage under account-first keys.
