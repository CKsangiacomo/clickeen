{
  "component": "menuactions",
  "syntax": "<button class='diet-btn-menuactions' data-size='{{SIZE}}' data-variant='{{VARIANT}}'>…</button>",
  "attributes": {
    "size": { "enum": ["xs", "sm", "md"], "default": "sm" },
    "variant": { "enum": ["neutral", "primary", "aimenuactions"], "default": "neutral" },
    "label": { "required": true },
    "icon": { "optional": true },
    "disabled": { "optional": true },
    "path": { "optional": true }
  },
  "binding": { "path": "instanceData[PATH]", "type": "string" },
  "previews": [
    {
      "id": "menuactions",
      "sizes": ["xs", "sm", "md"],
      "variants": ["neutral", "aimenuactions"],
      "spec": [
        "diet-btn-menuactions",
        "data-size='{{size}}'",
        "data-variant='{{variant}}'"
      ],
      "context": {
        "size": "sm",
        "variant": "neutral",
        "label": "Action",
        "icon": "square.on.square",
        "path": ""
      },
      "sizeContext": {
        "xs": { "labelClass": "label-xs" },
        "sm": { "labelClass": "label-s" },
        "md": { "labelClass": "label-m" }
      }
    }
  ]
}
