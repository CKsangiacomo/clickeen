{
  "name": "@clickeen/tokyo-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --routes \"\"",
    "test:auth-config": "tsx tests/run-auth-config.ts",
    "test:content-slots": "tsx tests/run-content-slot-overlays.ts",
    "test:publication-capacity": "tsx tests/run-publication-capacity.ts",
    "test:widget-defaults": "tsx tests/run-account-widget-defaults.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickeen/ck-contracts": "workspace:*",
    "@clickeen/ck-policy": "workspace:*",
    "@clickeen/l10n": "workspace:*",
    "@clickeen/widget-foundation": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260305.0",
    "tsx": "^4.20.6",
    "wrangler": "4.69.0"
  }
}
