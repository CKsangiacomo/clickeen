{
  "name": "@clickeen/tokyo-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 4001",
    "deploy": "node ../scripts/infra/ensure-queues.mjs --write --queue instance-render-snapshot-cloud-dev && wrangler deploy",
    "test": "rm -rf /tmp/clickeen-tokyo-worker-tests && esbuild src/route-helpers.test.ts src/domains/render/overlays.test.ts src/domains/render/instance-index.test.ts --bundle --platform=node --format=esm --outdir=/tmp/clickeen-tokyo-worker-tests && node --test /tmp/clickeen-tokyo-worker-tests/route-helpers.test.js /tmp/clickeen-tokyo-worker-tests/domains/render/overlays.test.js /tmp/clickeen-tokyo-worker-tests/domains/render/instance-index.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickeen/ck-contracts": "workspace:*",
    "@clickeen/ck-policy": "workspace:*",
    "@clickeen/l10n": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251228.0",
    "wrangler": "^4.54.0"
  }
}
