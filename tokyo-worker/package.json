{
  "name": "@clickeen/tokyo-worker",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy --routes \"\"",
    "test:auth-config": "tsx tests/run-auth-config.ts",
    "test:clk-live": "tsx tests/run-clk-live-locale-serving.ts",
    "test:public-loader": "tsx tests/run-public-loader-contract.ts",
    "test:widget-cache": "tsx tests/run-widget-cache-lifecycle.ts",
    "test:page-contract": "tsx tests/run-page-contract.ts",
    "test:page-serving": "tsx tests/run-page-public-serving.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@clickeen/ck-contracts": "workspace:*",
    "@clickeen/ck-policy": "workspace:*",
    "@clickeen/l10n": "workspace:*",
    "@clickeen/widget-shell": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251228.0",
    "tsx": "^4.20.6",
    "wrangler": "^4.54.0"
  }
}
