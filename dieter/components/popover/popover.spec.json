{
  "component": "popover",
  "syntax": "<div class='diet-popover' data-width='row|wide|extra-wide'>…</div>",
  "attributes": {
    "headerLabel": { "optional": true },
    "width": { "enum": ["row", "wide", "extra-wide"], "default": "row" }
  },
  "defaults": [
    {
      "id": "popover",
      "sizes": ["default"],
      "spec": ["diet-popover"],
      "context": {
        "size": "default",
        "width": "row",
        "headerLabel": "Property",
        "bodyLineOne": "Caller-provided content belongs in the Popover body.",
        "bodyLineTwo": "Popover owns the floating surface and its structure."
      }
    }
  ]
}
