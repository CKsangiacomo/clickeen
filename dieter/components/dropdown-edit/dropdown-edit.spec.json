{
  "component": "dropdown-edit",
  "description": "Inline rich text editor with popover workflow",
  "syntax": "<tooldrawer-field type='dropdown-edit' label='{{LABEL}}' size='{{SIZE}}' path='{{PATH}}' placeholder='{{PLACEHOLDER}}' />",
  "attributes": {
    "type": {
      "value": "dropdown-edit",
      "required": true,
      "description": "Component type identifier"
    },
    "label": {
      "type": "string",
      "required": true,
      "description": "Title shown in the editor popover header",
      "example": "Heading"
    },
    "headerLabel": {
      "type": "string",
      "optional": true,
      "description": "Label rendered in the shared popover header"
    },
    "headerIcon": {
      "type": "string",
      "optional": true,
      "description": "Icon token rendered to the right of the header label"
    },
    "size": {
      "type": "enum",
      "values": ["sm", "md", "lg"],
      "default": "md",
      "required": false,
      "description": "Editor size - sm (20px), md (24px), lg (32px) height"
    },
    "path": {
      "type": "string",
      "required": true,
      "description": "Data path in instanceData for binding (e.g., 'heading', 'description')"
    },
    "placeholder": {
      "type": "string",
      "default": "The quick brown fox jumps over the lazy dog",
      "required": false,
      "description": "Default text content shown in preview and editor"
    },
    "allowLinks": {
      "type": "boolean",
      "default": true,
      "required": false,
      "description": "Whether to allow hyperlinks in the editor content. Use false for non-interactive text."
    }
  },
  "features": {
    "richTextFormatting": ["Bold", "Italic", "Underline", "Strikethrough"],
    "linkPanel": "Add hyperlinks with URL validation, new tab option, and nofollow attribute",
    "aiSuggestions": [
      "Fix grammar",
      "Shorten (keep meaning)",
      "Expand (add detail)",
      "Simplify reading",
      "Make professional",
      "Make friendly",
      "Make persuasive"
    ]
  },
  "defaults": [
    {
      "id": "dropdown-edit",
      "sizes": ["sm", "md", "lg"],
      "spec": ["diet-dropdown-edit", "data-size='{{size}}'"],
      "context": {
        "size": "sm",
        "id": "dropdown-edit-{{size}}",
        "label": "Label",
        "headerLabel": "Label",
        "headerIcon": "textformat.size.larger",
        "placeholder": "The quick brown fox jumps over the lazy dog",
        "path": ""
      },
      "sizeContext": {
        "sm": { "labelClass": "label-xs", "bodyClass": "body-xs" },
        "md": { "labelClass": "label-s", "bodyClass": "body-s" },
        "lg": { "labelClass": "label-m", "bodyClass": "body-m" }
      }
    }
  ],
  "dataBinding": {
    "automatic": true,
    "description": "Editor content automatically syncs to instanceData[path] as HTML string with inline formatting"
  }
}
