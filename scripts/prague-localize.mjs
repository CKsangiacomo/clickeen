console.log('[prague-localize] TODO: translate Prague markdown + widget instance JSON into per-locale pages.');
console.log('[prague-localize] This is intentionally a stub while we finalize the localization pipeline (D1 cache + CI jobs).');

