# PRD 129 — Clickeen Widget Software And Instance Lifecycle

Status: **LOCAL LIFECYCLE CORRECTION IMPLEMENTED — CLOUD-DEV VERIFICATION PENDING**

Owner: Clickeen product owner/architect

Date: 2026-08-17

## 1. What This PRD Fixes

Clickeen already has Widget software, editable account instances, Bob, Roma,
Tokyo-worker, Publish, and public serving.

Before this local PRD 129 implementation, the repository mixed four different
product actions into one client-rendering and materialization workflow. PRD
129 separates them:

```text
1. Start an unsaved browser-memory draft from Widget/account defaults
2. Edit that draft and explicitly Save its editable source
3. Publish the saved instance when the user chooses and the account permits it
4. Serve the files generated by Publish
```

The largest correction inside Edit is preview. Bob must preview the current
browser-memory draft from the Widget software used for editing. It must not
load, parse, rewrite, or execute an instance's stored serving package. A
never-published instance has no serving package and must still open and preview
normally.

This is an architecture correction to the existing product. It is not a new
product, an FAQ-only architecture, or a compatibility project.

## 2. Product Truth

### 2.1 Widget software

A Widget is complete product software that performs one specific job.

Every Widget contains:

- the complete shared-and-Core document composition it uses;
- mandatory Core HTML, CSS, and JavaScript;
- its default state and Bob editing declarations;
- its customer-content declarations;
- its bindings to shared Clickeen capabilities and account policy;
- its internal Discovery declaration; and
- its Widget-owned product copy.

The Widget owns its product meaning. It uses Bob, Roma, Tokyo-worker, Dieter,
localization, policy, and every other Clickeen capability through the same
system contracts used by every applicable Widget.

### 2.2 Editable instance

An instance is one account's customized use of one Widget.

Its editable source contains the exact saved values for shared Stage, Pod,
Header, typography, appearance, and applicable shared features together with
the Widget's exact Core values and base-locale customer content.

The editable instance is not Widget source code and is not public browser
code.

### 2.3 Saved source

New creates no account instance and writes no source. The first explicit Save
mints the instance identity and writes the exact current Bob draft as the first
saved source. Later Save commands update that source while the user edits in
Bob. Duplicate remains an explicit operation that immediately creates a saved,
unpublished copy.

Editable source is one atomic storage artifact:

```text
instance.source.json
```

It contains the exact instance metadata, config, and base-locale content as one
logical source record. Save and Rename each replace that source with one R2
PUT, so no reader can observe a config/content split or a partially updated
editable instance.

On first Save, Tokyo-worker writes the initial unpublished `serve-state.json`
first and writes `instance.source.json` last. Only an exact
`instance.source.json` key makes an instance visible to inventory and source
reads. A failed first Save therefore cannot expose a half-created instance; an
orphan pre-commit serve-state object is not instance truth.

Delete uses the exact inverse of that visibility law. Inside the same account
command coordinator, deletion of the instance's exact `instance.source.json`
is the logical Delete commit. Once the source anchor is gone, the instance is
absent from inventory and authoritative source/public reads. Tokyo's default
entrypoint then schedules deletion of the residual instance prefix, including
`serve-state.json` and overlays, through `waitUntil`; it also schedules the
ordinary product-inert cache eviction. Residual cleanup completion, rejection,
or failure cannot delay, reverse, or reclassify the Delete result.

The prior `instance.config.json` plus `instance.content.json` storage topology
is not compatibility input. Existing pre-GA cloud-dev instances require an
explicit cutover or recreation decision before the corrected runtime can own
them. No compatibility reader, merge-on-read, or remote conversion is part of
this pass.

Save is persistence inside Edit. It is not Publish and it does not generate
public browser files.

### 2.4 Published package

Publish is a separate user action.

Only an allowed Publish asks Roma's materializer to generate the browser files
for the exact saved instance:

```text
index.html
styles.css
runtime.js
```

All three files are mandatory. Together they are one published package.
They are logical public file members, not three independently committed
storage objects. Tokyo-worker commits the published status, `publishedAt`, and
the exact `{ indexHtml, stylesCss, runtimeJs }` package together in the
instance's one `serve-state.json` artifact.

### 2.5 Public serving

Tokyo-worker serves the stored published package. A public request never
creates, edits, saves, publishes, or rebuilds an instance.

## 3. The Four SubPRDs

| SubPRD | Product action | Starts with | Ends with |
| --- | --- | --- | --- |
| 129A | Widget Software And Start | Widget software plus New, Duplicate, or Template starting truth | New opens one unsaved draft; Duplicate opens one saved unpublished copy |
| 129B | Edit And Save | Widget software plus one editable instance opened in Bob | Live source-only draft preview plus updated saved source; Bob remains available for more editing |
| 129C | Publish | Exact saved source plus explicit Publish intent | One stored published package, or one exact denial with no publication change |
| 129D | Serve | Public request plus stored publication truth | Browser receives the stored HTML, CSS, and JavaScript |

The handoff is deliberately simple:

```text
Widget software
  -> Start unsaved draft
  -> Edit and Save editable source
  -> Publish saved source
  -> Serve stored published files
```

No SubPRD may perform another SubPRD's job.

## 4. Start: New, Duplicate, And Template

Start has three modes:

- **New** composes the Widget's defaults plus the account's shared Widget
  defaults into Bob browser memory. It creates no instance identity, source
  object, package object, serve state, or inventory row before Save.
- **Duplicate** starts from another exact saved instance, creates a new
  unpublished instance, and opens the duplicate in Bob.
- **Template** starts from a selected normal saved instance owned by the
  CLICKEEN admin account. The normal listed/admin-selected instances are the
  Template catalog; there is no separate template model, table, source shape,
  or storage root. The cross-account Duplicate that creates the new customer
  instance is follow-on implementation through the existing instance
  authorities.

New and the saved-copy modes deliberately end differently:

```text
New -> unsaved browser-memory draft -> Bob
Duplicate/Template copy -> saved unpublished editable instance -> Bob
```

First Save writes New's editable source and unpublished publication truth.
Duplicate writes its saved copy immediately. Neither operation generates
`index.html`, `styles.css`, or `runtime.js`.

## 5. Edit And Save

Bob owns one complete browser-memory draft and exactly one account-bound
command: Save.

The user can edit, preview, undo, and Save repeatedly. Product Copilot and
Translations require a saved instance identity, so a New draft shows **Save
first** for those surfaces until first Save returns and is adopted.
Workspace preview combines the deploy-built Widget software with that draft in
Bob's isolated editing surface. It does not consume Tokyo-worker's stored
`index.html`, `styles.css`, or `runtime.js`, and public `runtime.js` contains no
Bob editing protocol.

Save persists the current editable source through Roma. With no instance ID it
creates the first saved source. Roma's existing `201` Save result returns both
the new ID and the exact current account `baseLocale` persisted with that
source. Roma adopts the ID for its route/inventory fact, while Bob consumes the
same existing command result and adopts the ID plus `baseLocale` into session
metadata and `translationSetup` in place. There is no Bob reopen and no new
message. With an existing ID, Save updates source. Neither path invokes the
materializer, replaces public files, changes publication state, or generates
localization output.

The Save ingress is identity-specific. Bob sends `widgetType` with `config`
only for first Save, when no stored instance exists yet. For an existing
instance, Bob sends a config-only body. Roma's external route admits the body
and `config` as records, loads the exact account-scoped saved-instance list fact
from Tokyo, and uses Tokyo's stored `widgetType` to select the compiled artifact
that prepares the source update. Existing Save neither accepts nor compares a
caller-owned `widgetType`; no caller `widgetType` comparison or revalidation
occurs. Tokyo's stored ownership fact is the direct artifact-selection input.

This exact-result adoption keeps the ordinary first-Save session coherent. It
does not serialize a simultaneous first Save against an account-locale
`PATCH`; that separate first-Save/base-locale transition race is not solved or
claimed solved by this response change.

If a published instance is edited and saved, the currently published package
remains public until the user explicitly publishes again.

## 6. Publish

Publish, Republish, and Unpublish are Roma commands rendered in Roma's Widgets
inventory and in one slim Roma-owned bar above Bob. Bob receives no publication
state and emits no publication intent.

An allowed Publish:

1. reads the exact saved source;
2. invokes Roma's materializer once;
3. generates complete `index.html`, `styles.css`, and `runtime.js`;
4. asks Tokyo-worker to commit the generated strings and publication truth in
   one `serve-state.json`; and
5. makes that one stored publication artifact the public truth for the
   instance.

Tokyo-worker's existing `AccountPublicationCoordinator` is one per account and
serializes every mutation of an existing instance: Save, Rename, Publish,
Unpublish, and Delete. The first Save of a New draft creates a new identity and
is not an existing-instance command. If any coordinated command arrives while
another is active, it receives
`409 coreui.errors.instance.commandInProgress`; it performs no mutation and is
not queued or retried automatically.

Roma sends Publish the exact `sourceUpdatedAt` it read before materialization.
Inside the same coordinator, Tokyo-worker compares that coordinate with the
current source `updatedAt` before capacity or publication commit. A Save or
Rename that changed the source first therefore produces
`409 coreui.errors.instance.sourceChanged`, and the prepared package is not
committed. Tokyo is the single writer for these revision coordinates: every
existing-instance Save or Rename makes `updatedAt` strictly later than its
prior `updatedAt` and `publishedAt`, and Publish/Republish makes `publishedAt`
strictly later than both the exact `sourceUpdatedAt` it commits and the prior
`publishedAt`. Roma can compare the two facts directly; every successful
Republish refreshes its receipt even within one millisecond, with no UI
validator or timestamp repair path.

Publish success is the stored package/publication commit; Unpublish success is
the stored publication transition. After that result, Tokyo's default Worker
entrypoint may schedule its own deterministic account-instance Cache-Tag
eviction through `waitUntil`. Missing cache context, synchronous failure,
rejection, `success:false`, or a never-settling eviction cannot alter, delay, or
reclassify the product result. Roma and the user have no cache failure state,
banner, retry, reconciliation, rollback, queue, or polling workflow.

A Publish denied by Roma's fast local capacity precheck performs no
materialization. A request that reaches Tokyo-worker but receives
`commandInProgress`, `sourceChanged`, or the final capacity denial may already
have materialized transient package strings in Roma memory, but it persists no
package or publication state and does not change saved source or the currently
served package.

Publish never silently Saves a dirty Bob draft. Roma disables Publish or
Republish with **Save first** while Bob is dirty; Unpublish remains available
because it does not consume the draft.

## 7. Serve

Tokyo-worker serves the already-stored package for a published instance.

Serving does not call Bob, Roma, the materializer, a model, or Widget source.
It does not rebuild HTML, CSS, or JavaScript.

For a selected locale, Tokyo-worker expresses every present stable-coordinate
value from the exact stored locale overlay in the returned semantic HTML
before client JavaScript. Reorder follows saved identity; a newly added
identity remains intentional untranslated saved source content until Generate
Translations; and a deleted identity has no current node. Editable attributes
use the exact authored `data-ck-content-attribute` target. Tokyo-worker does
not create a second locale package, read old positional keys through a
compatibility path, or migrate overlays on a visitor request.

## 8. Free Model And Tiers

Widget access and public capacity are different products.

A Free user can:

- use every available Widget type;
- create multiple editable instances;
- duplicate, edit, preview, and Save them; and
- publish and serve one instance at a time.

The system capability `instances.published.max` owns public capacity. Free is
`1`; the system policy matrix owns every tier value.

The current matrix deliberately keeps Tier 1 at one published instance because
Tier 1 expands product features rather than public-instance capacity. Tier 2,
at five published instances, is intentionally the first multi-publish tier.

The prior `widgets.instances.max` Create-time gate conflicted with this model.
The local implementation removes that capability and gate rather than renaming
or repurposing it.

Widget-bound editing limits are separate. They apply once when the user tries
the governed edit. They are not repeated at Save or Publish.

### 8.1 Account Lifecycle

An account tier/status transition is account policy, not a fifth Widget
lifecycle action. It does not change what a Widget or editable instance is.
Every editable instance and its exact saved source remain intact; lower policy
applies at the next governed user action.

The settled suspended-account lifecycle provides day 0-30 public-serving
grace, automatic free-tier serving materialization at day 30 when recovery has
not occurred, and automatic account-root deletion at day 90 if the account is
still unrecovered. The account operation owns the exact allowed published set
and any tier-dependent package result. Public Serve does not inspect tier or
choose what stays live.

The scheduled lifecycle runner and complete account-root deletion operation
remain documented Account Management implementation gaps. They are follow-on
account work, not a PRD 129 Publish/Serve blocker.

## 9. Discovery And Enable SEO/GEO

Every Widget contains an internal `discovery.json` declaration. It tells the
system:

- what the Widget is;
- the Widget's Clickeen-owned baseline Discovery defaults;
- which exact customer-content parts matter to search and answer systems; and
- how those content parts relate.

The user does not edit `discovery.json`, and Bob does not generate an SEO
editor from it.

The existing shared account capability controls **Enable SEO/GEO**:

- every tier receives the Widget's Clickeen baseline title and description;
- Tier 2 and above may additionally enable optimization from their exact saved
  content.

The system owns tier truth and the shared control. The Widget owns its
Discovery declaration and baseline defaults. Roma's materializer uses both
only during Publish.
Only the materializer writes the technical discovery output included in the
published package. Serve only returns that stored result.

## 10. Authorities

| Authority | Responsibility |
| --- | --- |
| Widget software | Widget meaning, defaults, declarations, Discovery, Core HTML/CSS/JavaScript, and Widget-owned copy |
| Bob | Browser-memory editing, source-only isolated preview, undo, Product Copilot changes, and Save only |
| Roma | Current-account New-draft composition, first/later Save routing, Publish/Republish/Unpublish, policy, publication UI, and Bob-opening commands |
| Roma materializer | Sole generator of instance-specific `index.html`, `styles.css`, and `runtime.js`, during allowed Publish only |
| Tokyo-worker | Physical account-instance storage, publication truth, and public serving of stored files |
| Dieter | Shared design tokens and UI primitives |
| Other Clickeen capabilities | One shared implementation used the same way by every applicable Widget |

No service may acquire Widget-specific meaning. No second editor, materializer,
storage root, or serving path is authorized.

## 11. Local All-Widget Implementation State

The local implementation now applies the four separate actions to Big Bang,
Cards, Countdown, FAQ, and Logo Showcase:

- New opens an unsaved draft without identity or storage; first Save writes its
  unpublished editable source;
- Duplicate immediately writes one saved unpublished copy;
- Duplicate opens the new instance in Bob;
- Bob opens and previews from the compiled Widget software plus its current draft,
  without reading a stored public package;
- one generic Bob edit decision applies Widget-bound limits before draft mutation,
  and one Roma Popup composes the exact denial;
- Save updates editable source only;
- explicit allowed Publish alone invokes Roma's generic materializer and stores
  complete `index.html`, `styles.css`, and mandatory `runtime.js` through
  Tokyo-worker;
- Roma's fast publication-capacity precheck is finalized by Tokyo-worker's
  per-account existing-instance command coordinator and one atomic
  `serve-state.json` publication write: an overlapping Save, Rename, Publish,
  Unpublish, or Delete receives `409 commandInProgress`; a Publish prepared
  from a superseded source revision receives `409 sourceChanged`; a later
  over-capacity first Publish receives `402 UPGRADE_REQUIRED`; and every loser
  persists nothing; and
- Tokyo-worker serves the stored package and applies an exact requested locale
  overlay by stable identity to declared HTML content or authored attribute
  slots at the Edge before JavaScript.

All five current Widgets now use the canonical `widget.html`, `core/`,
`discovery.json`, `limits.json`, `labels/en.json`, and `upsell/en.json` source
topology. The retired flat CSS/client/label paths are absent. The generator has
one universal source contract and no compatibility discriminator or Widget
branch.

No suspended-account lifecycle runner or complete account-root deletion
operation was added. Their settled grace/materialization/deletion law and
current implementation gap remain with Account Management rather than
blocking the Widget lifecycle.

## 12. Existing Coordinates

Widget software remains under:

```text
tokyo/product/widgets/{widgetType}/
```

Account instances remain under:

```text
accounts/{accountPublicId}/instances/{instanceId}/
  instance.source.json
  serve-state.json
  overlays/
    locales/
      {locale}.json
```

For a published instance, `serve-state.json` is the single publication commit
artifact and contains:

```text
status: published
publishedAt
publicPackage:
  indexHtml
  stylesCss
  runtimeJs
```

There are no separate `index.html`, `styles.css`, or `runtime.js` storage
objects. The public URLs with those filenames remain unchanged; Tokyo-worker
selects the corresponding exact string from `publicPackage`. A never-published
instance has unpublished serve state and no public package. Exact publication
replacement mechanics belong to 129C.

## 13. All Current Widgets

PRD 129 defines one architecture for every Widget. Big Bang, Cards, Countdown,
FAQ, and Logo Showcase are the complete current local implementation and proof
set. Prior cloud-dev evidence covers the earlier PRD 129 baseline, but the
current New/Save/publication-coordinator/atomic-package/cache correction has not
been deployed or verified there. Clickeen is pre-GA, so neither the old flat
Widget source shape nor the prior separate-package-object storage shape has a
compatibility workflow. Owner cloud-dev QA remains pending.

## 14. Execution Order Used

```text
129A Widget software and non-persisting New
-> 129B source-based Bob preview and source-only Save
-> 129C Roma-only Publish/Republish/Unpublish and package generation
-> 129D stored-package and selected-locale serving
-> product-inert background cache eviction
-> canonical documentation and local verification
-> local reconciliation and independent V1-V8 audit
```

The same implementation pass also contains a separate, non-load-bearing
closed-system trust cleanup: Roma exact-result consumption, Widget Defaults
compiled-artifact consumption, Tokyo asset-policy deduplication, the governed
Product Copilot/San Francisco handoff, and Prague embed availability. Those
deletions do not redefine or gate the four lifecycle phases; they have their
own focused checks before the final audit.

The implementation does not execute Template cross-account duplication, the
account lifecycle runner/deletion operation, deployment, or remote product-data
work.

## 15. Preservation

The all-Widget implementation preserves each Widget's existing product
behavior, saved values, customer content, ToolDrawer meaning, localization
coordinates, and shared
Stage, Pod, Header, typography, appearance, branding, social-share,
locale-switcher, asset, and account capabilities unless an exact change is
approved in the relevant SubPRD.

Architecture correction is not permission to redesign Widget functionality.

## 16. Local Completion Reconciliation

The local all-Widget implementation is complete when:

1. all four SubPRDs are approved;
2. all five current Widgets follow the same Widget contract;
3. New opens an unsaved Bob draft and leaving before Save writes nothing;
4. Duplicate opens the new duplicate in Bob;
5. every current Widget opens and previews source-only instances in Bob
   without a stored serving package;
6. the Workspace contract universally uses deploy-built Widget software plus
   the one current draft and never uses public `runtime.js` as an editor
   renderer;
7. first Save creates exact source, commits `instance.source.json` last, and
   returns its exact new ID and persisted `baseLocale` for in-place Bob session
   adoption; later Save/Rename each replace that one source artifact
   atomically, and none of those source operations generates public files;
8. Publish alone generates the mandatory HTML/CSS/JavaScript package;
9. existing-instance Save/Rename/Publish/Unpublish/Delete serialize through
   one per-account coordinator, and Publish enforces exact source revision plus
   account publication capacity before one atomic publication write;
10. Save after Publish leaves the current public package unchanged;
11. Serve returns stored published files without rebuilding them;
12. Discovery is internal and Publish alone generates its technical output;
13. Serve does not calculate account tier or downgrade policy;
14. no shared service contains a Widget-specific branch;
15. no retired stored-package preview or flat Widget client workflow survives under
    another name or wrapper;
16. Bob contains no Publish, Republish, Unpublish, publication receipt, or
    public action;
17. Roma inventory and Roma's Builder bar consume one publication control and
    one result path;
18. cache eviction cannot reach a product response or product-visible state;
19. current operator documentation distinguishes local implementation from
    deployed runtime truth; and
20. an independent V1-V8 audit passes.

The Template catalog is normal listed CLICKEEN-admin saved instances;
cross-account Duplicate is separate follow-on implementation and introduces no
template model. The account lifecycle runner/full deletion, deploy, cloud-dev
proof, and remote product-data work are also outside this local completion.

## 17. Required Final V1-V8 Audit

| ID | Required result | Reason |
| --- | --- | --- |
| V1 | Pass | No missing source, tier, locale, Template, package, or stable-coordinate truth is replaced with an invented value; existing Save uses Tokyo's stored `widgetType` rather than a caller value or fallback, and intentional untranslated source content remains exact saved truth. |
| V2 | Pass | No action silently repairs or rewrites saved state. |
| V3 | Pass | New, first/later Save, source-only preview, Publish, and Serve each retain their required input and output; first Save alone carries `widgetType`, while existing Save carries config and consumes the account-scoped stored instance fact. |
| V4 | Pass | Edit limits remain at the governed edit; Tokyo-worker's lifecycle-fenced per-account coordinator serializes existing-instance Save/Rename/Publish/Unpublish/Delete, and exact `sourceUpdatedAt` plus capacity enforcement prevent stale or over-capacity publication. |
| V5 | Pass | Exact `instance.source.json` is the visibility, creation, and deletion-anchor fact; a valid unpublished source-only instance is distinct from an incomplete first-Save prefix, unreachable post-Delete residual bytes, missing truth, or corrupt state. |
| V6 | Pass | New writes nothing; Save, Rename, Publish, Delete, command contention, source contention, final capacity contention, Serve, and stable-identity add/reorder/delete each report only their own exact result; the one publication artifact cannot partially commit, and residual cleanup/cache eviction cannot masquerade as product failure. |
| V7 | Pass | No compatibility path, stored-package preview, or renamed `widget.client.js` workflow is authorized. |
| V8 | Pass | Verification remains offline evidence, not product-runtime machinery. |

This table states the required result. The independent post-implementation
audit is the implementation evidence.

## 18. Reconciliation State

```text
PRD writing: reconciled to the corrected local four-phase implementation
architecture implementation: corrected locally; prior PRD 129 baseline remains the last cloud-dev deployment
public multi-locale switcher: Edge-authored exact locale options present in cloud-dev
runtime code: local uncommitted lifecycle/coordinator/atomic-publication/cache correction; no deploy in this pass
account product data: no remote product-data work performed in this pass; previously documented cloud-dev state remains unchanged
atomic editable source: implemented locally as one instance.source.json containing metadata/config/content; Save/Rename replace it in one PUT
first-Save visibility: initial unpublished serve-state writes first and instance.source.json commits last; only exact source-record keys enumerate as instances
first-Save result adoption: the existing 201 Save result returns the exact new instanceId and persisted account baseLocale; Bob adopts both into session metadata and translationSetup without reopen or a new message; implemented locally, cloud-dev verification pending; this does not serialize or solve a simultaneous first-Save/account-locale PATCH race
Save ingress ownership: Bob sends widgetType only on first Save and sends config-only for an existing instance; Roma admits record config, loads Tokyo's exact account-scoped saved-instance list fact, and selects the compiled artifact from its stored widgetType without caller widgetType comparison/revalidation; implemented locally, cloud-dev verification pending
instance Delete commit: coordinated deletion of exact instance.source.json is the logical product result; serve-state/overlay prefix cleanup and cache eviction are scheduled afterward through waitUntil and are product-inert; implemented locally, cloud-dev verification pending
legacy cloud-dev source topology: every saved instance using instance.config.json plus instance.content.json requires an explicit pre-GA source cutover or recreation decision; no compatibility input or remote action in this pass
Tokyo git-authored product root: synced; 15 retired flat/legacy objects deleted and verified 404
main push: not performed for the current correction
deploy: not performed for the current correction
live product: prior PRD 129 baseline remains deployed; corrected New/Save/Roma-publication/cache behavior requires cloud-dev verification and owner QA
retired flat Widget source paths: removed and inactive in deployed code
Template catalog: normal listed CLICKEEN-admin saved instances
Template cross-account Duplicate: not implemented follow-on
account suspension lifecycle runner/full deletion: documented follow-on account work, not a PRD 129 blocker
atomic account publication transition: implemented locally as one `serve-state.json` write containing status, publishedAt, and exact logical publicPackage; cloud-dev verification pending
account coordinator: implemented locally as one lifecycle-fenced Tokyo Durable Object per account serializing existing-instance Save/Rename/Publish/Unpublish/Delete; no durable policy/count/publication truth
coordinator contention: `409 coreui.errors.instance.commandInProgress`; no operation, queue, poll, or automatic retry
publish source contention: exact sourceUpdatedAt mismatch returns `409 coreui.errors.instance.sourceChanged`; no publication write
revision coordinates: local Tokyo single-writer implementation makes Save/Rename updatedAt strictly later than prior updatedAt and publishedAt, and Publish/Republish publishedAt strictly later than both committed sourceUpdatedAt and prior publishedAt
stable scalar/repeated overlay identity: present in cloud-dev
authored exact attribute localization: present in cloud-dev
pre-GA positional-overlay compatibility path: absent; explicit Generate/delete cutover is required for previously stored positional overlays
stored positional-overlay Generate/delete cutover: pending
pre-GA atomic-publication cutover: deploy the corrected runtime, explicitly cut over or recreate every legacy saved instance, then explicitly Publish/Republish each instance intended to remain published so `serve-state.json` receives its logical publicPackage; no separate-object compatibility fallback exists
republish of currently published cloud-dev instances: pending; no Republish or other remote cutover work performed in this pass
cache eviction: local source schedules the exact account-instance Cache-Tag after owning mutations through `waitUntil`; product results never await or inspect it
cache policy: local source uses `public, max-age=60, s-maxage=300, must-revalidate` for generated public package responses
cache live QA: intentionally not a product acceptance gate; no live cache verification performed in this pass
independent V1-V8 audit: pending after all local slices finish
```
