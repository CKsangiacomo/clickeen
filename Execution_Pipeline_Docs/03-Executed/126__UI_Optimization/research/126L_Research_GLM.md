# 126L — Source research: Design-system governance tooling (GLM, Phase-1 step 3)

> GLM independent pass. **Not converged.**

## Material 3
- M3 is a published spec — no governance tool. The platform library IS the enforcement. No "reveal cockpit" concept.

## Apple HIG
- Apple's design system IS the platform. No governance tool — the OS enforces adoption.

## OpenAI UI
- Submission review is the governance. No live reveal cockpit.

## Industry analogs (not the three north stars, but relevant)
- **Style Dictionary** (Amazon) — multi-platform token build pipeline. clickeen's `build-dieter.js` is a lightweight version.
- **Supernova / Zeroheight** — design-system documentation + governance platforms. Live preview + token editing + versioning. DevStudio is a custom, lightweight version of these.

## Cross-source synthesis
- DevStudio is UNIQUE — none of the three north stars have a governance cockpit. clickeen's model (reveal derived truth + steer through validated commits) is more engineered than M3/HIG/OpenAI. The gaps (no attribution, regex validation, PR-only verify) are clickeen-specific pipeline issues, not missing industry patterns.

— end GLM research, 126L.
