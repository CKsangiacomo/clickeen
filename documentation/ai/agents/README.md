# AI Agents

STATUS: REFERENCE - MUST MATCH RUNTIME

This folder documents real Clickeen agent homes.

Live agent homes:

```text
agents/product-copilot/
agents/translation-agent/
```

An agent home is a deployed/operator runtime that owns reasoning for a specific
structured artifact. It calls San Francisco only for governed model execution.

## Built Agents

| Agent | Code | Product artifact |
| --- | --- | --- |
| Product Copilot | `agents/product-copilot/` | Builder draft artifact |
| Translation Agent | `agents/translation-agent/` | Saved instance content and locale overlay files |

## Planned Agents

Future agents live under `Planned_Agents/` until code/runtime exists. Planned
agent docs are not current authority and must not be referenced as built
runtime.

Current planned agents:

- DevOps Agent
- GTM Agent
- UX Writer Agent
- SEO/GEO/AEO Agent
- SDR Copilot
